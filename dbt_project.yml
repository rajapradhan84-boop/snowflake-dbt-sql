name: snowflake_dbt_sql
version: '1.0'
config-version: 2

profile: snowflake_dbt_sql

source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_modules"

models:
  snowflake_dbt_sql:
    +materialized: view
    marts:
      +materialized: table
